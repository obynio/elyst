<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

    <ImageView
        android:id="@+id/imageViewIcon"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/textViewWOneTitle"
        android:src="@drawable/app_icon"/>

    <TextView
        android:id="@+id/textViewWOneTitle"
        android:text="Welcome to Elyst"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_marginTop="10dp"
        android:textSize="30dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    <TextView
        android:id="@+id/textViewWTwoFooter"
        android:text="Never miss a thing"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/textViewWOneTitle"
        android:layout_marginTop="10dp"
        android:textSize="18dp"
        android:textColor="#9c9c9c"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <ImageButton
        android:id="@+id/imageButtonFabWOne"
        android:layout_width="@dimen/fab_button_diameter"
        android:layout_height="@dimen/fab_button_diameter"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="16dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/fab_shape"
        android:src="@mipmap/fab_ic_forward"
        android:tint="@android:color/white"/>

</RelativeLayout>